# گزارش کار شبیه‌ساز حافظه کش

## چکیده
این گزارش به بررسی عملکرد یک شبیه‌ساز حافظه کش می‌پردازد که با هدف شبیه‌سازی و تحلیل رفتار انواع مختلف حافظه کش طراحی شده است. در این پروژه، تأثیر پارامترهای مختلف کش مانند اندازه کش، اندازه بلوک، روش جایگزینی و میزان تداعی‌گری (associativity) بر کارایی سیستم بررسی شده است. همچنین الگوهای مختلف دسترسی به حافظه نیز مورد آزمایش قرار گرفته‌اند.

## مقدمه
حافظه کش یکی از مهم‌ترین اجزای سیستم‌های کامپیوتری مدرن است که با هدف کاهش شکاف سرعت بین پردازنده و حافظه اصلی طراحی شده است. عملکرد مؤثر کش می‌تواند تأثیر قابل توجهی بر کارایی کلی سیستم داشته باشد. این پروژه با هدف بررسی عمیق‌تر رفتار حافظه کش و عوامل مؤثر بر بهره‌وری آن طراحی و پیاده‌سازی شده است.

## اهداف پروژه
- شبیه‌سازی عملکرد حافظه کش با پارامترهای مختلف
- بررسی تأثیر الگوهای مختلف دسترسی به حافظه بر کارایی کش
- تحلیل و مقایسه روش‌های مختلف جایگزینی بلوک در کش
- بررسی اثر تغییر میزان تداعی‌گری و اندازه بلوک بر نرخ اصابت (hit rate)
- ارائه توصیه‌های بهینه برای پیکربندی کش برای انواع الگوهای دسترسی به حافظه

## معماری شبیه‌ساز

### کلاس CacheSim
کلاس اصلی شبیه‌ساز که پارامترهای زیر را می‌پذیرد:
- `csize`: اندازه کل کش بر حسب بایت
- `bsize`: اندازه هر بلوک بر حسب بایت
- `assoc`: میزان تداعی‌گری (عدد ۱ برای نگاشت مستقیم، -۱ برای تداعی‌گری کامل)
- `replace_rule`: سیاست جایگزینی (LRU، FIFO، LFU، RANDOM)

### پارامترهای محاسبه شده
- `num_blocks`: تعداد کل بلوک‌های کش
- `num_sets`: تعداد مجموعه‌ها در کش
- `set_size`: تعداد بلوک در هر مجموعه

### اجزای آماری
- شمارنده‌های اصابت (hits) و عدم اصابت (misses)
- آدرس‌های موفق و ناموفق برای استفاده در تصویرسازی
- زمان‌سنج برای الگوریتم‌های جایگزینی

## الگوریتم‌های جایگزینی
شبیه‌ساز چهار الگوریتم جایگزینی را پشتیبانی می‌کند:

1. **LRU (Least Recently Used)**: بلوکی که کمترین استفاده اخیر را داشته، جایگزین می‌شود.
2. **FIFO (First In First Out)**: اولین بلوکی که وارد شده، اولین بلوکی است که جایگزین می‌شود.
3. **LFU (Least Frequently Used)**: بلوکی که کمترین فراوانی استفاده را داشته، جایگزین می‌شود.
4. **RANDOM**: یک بلوک به صورت تصادفی برای جایگزینی انتخاب می‌شود.

## الگوهای دسترسی به حافظه
چهار الگوی مختلف دسترسی به حافظه برای آزمایش تعریف شده‌اند:

1. **متوالی (Sequential)**: دسترسی به آدرس‌های حافظه به صورت متوالی
2. **تصادفی (Random)**: دسترسی کاملاً تصادفی به حافظه
3. **حلقه‌ای (Loop)**: تکرار دسترسی به یک بخش کوچک از حافظه
4. **مجاورتی (Locality)**: دسترسی با تمرکز بر یک منطقه خاص از حافظه با گاهی پرش به مناطق دیگر

## روش‌های آزمایش

### آزمایش منفرد
یک آزمایش با پارامترهای کش مشخص و الگوی دسترسی مشخص انجام می‌شود و نتایج آن شامل نرخ اصابت و عدم اصابت نمایش داده می‌شود.

### مقایسه روش‌های جایگزینی
با ثابت نگه داشتن سایر پارامترها، چهار الگوریتم جایگزینی مختلف آزمایش و نتایج آن‌ها مقایسه می‌شوند.

### مقایسه میزان تداعی‌گری
میزان تداعی‌گری مختلف (نگاشت مستقیم، ۲-راهه، ۴-راهه، ۸-راهه، و تداعی‌گری کامل) با ثابت نگه داشتن سایر پارامترها آزمایش و مقایسه می‌شود.

### مقایسه اندازه بلوک
اندازه بلوک‌های مختلف (۱۶، ۳۲، ۶۴، ۱۲۸، ۲۵۶ بایت) آزمایش و مقایسه می‌شود.

### آزمایش جامع
ترکیبی از تمام پارامترهای ممکن (اندازه کش، اندازه بلوک، میزان تداعی‌گری، روش جایگزینی) برای هر الگوی دسترسی آزمایش می‌شود و بهترین پیکربندی برای هر الگو مشخص می‌شود.

## تصویرسازی نتایج
شبیه‌ساز قابلیت‌های تصویرسازی زیر را ارائه می‌دهد:

1. **نمودار دسترسی به حافظه**: نمایش گرافیکی تمام دسترسی‌ها به حافظه همراه با مشخص کردن اصابت‌ها و عدم اصابت‌ها با رنگ‌های متفاوت
2. **نمودار مقایسه‌ای**: مقایسه نرخ اصابت و عدم اصابت برای پارامترهای مختلف کش

## نتایج و تحلیل

### تأثیر روش‌های جایگزینی
- **LRU**: معمولاً در الگوهای با مجاورت محلی بالا عملکرد خوبی دارد
- **LFU**: در اجراهای طولانی با دسترسی‌های تکراری به داده‌های محدود مؤثر است
- **FIFO**: ساده‌ترین پیاده‌سازی با عملکرد نسبتاً خوب در اکثر موارد
- **RANDOM**: علی‌رغم سادگی، گاهی عملکرد قابل مقایسه با روش‌های پیچیده‌تر دارد

### تأثیر میزان تداعی‌گری
- افزایش میزان تداعی‌گری معمولاً باعث بهبود نرخ اصابت می‌شود
- بهبود عملکرد با افزایش تداعی‌گری دارای بازده نزولی است
- تداعی‌گری کامل لزوماً بهترین نتیجه را نمی‌دهد، به خصوص با در نظر گرفتن پیچیدگی پیاده‌سازی

### تأثیر اندازه بلوک
- بلوک‌های بزرگ‌تر از مجاورت فضایی بهتر بهره می‌برند
- بلوک‌های خیلی بزرگ می‌توانند منجر به اتلاف فضا و افزایش جریمه جابجایی شوند
- اندازه بهینه بلوک به الگوی دسترسی بستگی دارد

### بهترین پیکربندی برای هر الگو
نتایج آزمایش جامع نشان می‌دهد:

1. **الگوی متوالی**: کش بزرگ‌تر با بلوک‌های بزرگ و LRU بهترین عملکرد را دارد
2. **الگوی تصادفی**: تداعی‌گری بالاتر با بلوک‌های کوچک‌تر و LFU مؤثرتر است
3. **الگوی حلقه‌ای**: تداعی‌گری کامل با LRU بهترین عملکرد را دارد
4. **الگوی مجاورتی**: ترکیبی از اندازه کش متوسط، تداعی‌گری چند-راهه و LRU مناسب است

## نتیجه‌گیری
شبیه‌ساز حافظه کش ابزاری قدرتمند برای درک و تحلیل عملکرد حافظه کش ارائه می‌دهد. نتایج نشان می‌دهد که انتخاب پارامترهای بهینه کش بسیار وابسته به الگوی دسترسی به حافظه است. برخی از مشاهدات کلیدی:

- هیچ پیکربندی واحدی برای تمام الگوهای دسترسی بهینه نیست
- LRU به طور کلی از سایر روش‌های جایگزینی بهتر عمل می‌کند، به خصوص برای الگوهای با مجاورت بالا
- افزایش تداعی‌گری تا حد خاصی مفید است اما فراتر از آن بازده نزولی دارد
- اندازه بلوک باید متناسب با الگوی دسترسی انتخاب شود

## کاربردها و توسعه‌های آینده
- بهینه‌سازی پیکربندی کش برای برنامه‌های خاص
- توسعه الگوریتم‌های جایگزینی هوشمندتر
- شبیه‌سازی سیستم‌های چند سطحی کش
- بررسی سیاست‌های نوشتن (write policies) مختلف
- پیاده‌سازی کش با روش‌های پردازش موازی

## منابع
پروژه شبیه‌ساز حافظه کش با استفاده از زبان Python پیاده‌سازی شده و از کتابخانه‌های matplotlib برای تصویرسازی، numpy برای پردازش داده و tabulate برای نمایش جدول نتایج استفاده کرده است.

---

این گزارش یک خلاصه جامع از پروژه شبیه‌ساز حافظه کش است که مفاهیم، پیاده‌سازی، روش‌های آزمایش و نتایج آن را توضیح می‌دهد. شبیه‌ساز با ارائه ابزارهای تحلیلی مختلف، امکان بررسی دقیق عملکرد حافظه کش تحت شرایط مختلف را فراهم می‌کند.